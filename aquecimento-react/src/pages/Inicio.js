import React, { Component } from 'react';

import Header from '../components/Header';

export default class Inicio extends Component {
    render() {
        return (
            <div>
                <Header titulo="Aquecimento" />
                Aquecimento React
            </div>
        )
    }
}
